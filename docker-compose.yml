services:
  media-finder:
    build: .
    container_name: media-finder
    ports:
      - "3000:3000"       # 访问地址：http://NAS的IP:3000
    environment:
      PORT: "3000"
      NODE_ENV: production
      SERVE_STATIC_DIR: /app/public
    restart: unless-stopped
